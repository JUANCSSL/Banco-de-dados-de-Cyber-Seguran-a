CREATE TABLE `Users` (
  `user_id` int PRIMARY KEY,
  `department_id` int,
  `username` varchar(50),
  `passwordd` varchar(50),
  `email` varchar(50),
  `job_title` varchar(50),
  `status` varchar(50),
  `phone_Number` varchar(50)
);
